// This layout is no longer used — AppShell in app/layout.tsx handles sidebar.
// The (dashboard) route group exists only as a legacy artifact from initial scaffolding.
export default function DashboardLayout({ children }: { children: React.ReactNode }) {
  return children
}
